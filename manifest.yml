---
applications:
- name: crt-portal
  memory: 512M
  instances: 1
  buildpacks:
  - python_buildpack
  command: cd crt_portal && python manage.py migrate && gunicorn crt_portal.wsgi --workers 2
  services:
  - crt-db
  - crt-s3
  - VCAP_SERVICES